# PowerShell-RegPolParser
Bunch of functions to parse registry.pol files into human readable formats. Returns an array.

Usage: 
```powershell
$entries = @()
$entries = ParseRegPol "registry.pol"
```
